{
  "time": "2026-09-08T13:19:48.631194+00:00",
  "phase": "I timing only",
  "transition": "Median increase >15% and disjoint IQRs; require clean same-core endpoints; verify full huge backing where requested.",
  "conflict_replication": "Exact adjacent integer transition in both independent seeds; fixed-scrub tests additionally require both scrub patterns.",
  "conditional_geometry": "Exactly one repeated fixed-scrub count at >=2 strides; count times smallest agreeing tested stride lies in previously measured L2 capacity bracket. Report only a conditional candidate, never independently verified physical ways.",
  "physical_limit": "Mixed scrub/candidate averages do not independently establish complete upper eviction; smallest tested period may not be fundamental.",
  "capacity": "Report every interval inside prior envelope that overlaps qualifying transitions across both seeds and both page policies. Do not select one convenient transition or silently replace physical capacity.",
  "spatial": "Require clean same-core huge-backed below/at/above cohort with IQR gap >=2 native interval ticks after batch division. Larger footprint does not independently identify physical source level."
}
